<h2>Departments:</h2>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var department in Model)
    {
      <li><a href="/departments/@department.GetId()">@department.GetName()</a></li>
    }
  </ul>
}
<h4><a href="/departments/new">Add a new departments</a></h4>

<h4><a href="/">Return home</a></h4>
